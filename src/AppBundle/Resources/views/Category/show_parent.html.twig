{% extends 'base.html.twig' %}

{% block leftMenu %}


{% endblock %}

{% block body %}
     <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('parent_category', { 'name': entity.name }) }}">{{ entity.name }}</a></td>
             </tr>
        {% endfor %}
        </tbody>
    </table>

{{ render(controller('AppBundle:Category:auction_in_category', { 'category': app.request.get('name') })) }}

{% endblock %}

{% block javascripts %}
     <script src="{{ asset('assets/vendor/MyJS/test.js') }}"></script>
{% endblock %}

